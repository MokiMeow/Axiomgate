# Public Test Fixtures

Only synthetic, deterministic, sanitized fixtures belong here.

Each fixture must document:

- purpose;
- schema;
- expected result;
- generating source;
- absence of real credentials/user data;
- version.

Private or raw fixtures belong under `.local/`.
